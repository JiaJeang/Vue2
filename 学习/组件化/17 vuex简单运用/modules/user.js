const state = {
  userInfo: {
    name: 'Jeang',
    age: 20
  },
  value: 200
}
const mutations = {}
const actions = {}
const getters = {}

export default {
  // 开启命名空间
  namespaced: true,
  state,
  mutations,
  actions,
  getters
}
